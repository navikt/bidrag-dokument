####################################################################
# 
# Profiles: All
#							
####################################################################

server:
  servlet:
    context-path: /bidrag-dokument
management:
  endpoints:
    web:
      exposure:
        include: "*"

ISSO_ISSUER_URL: https://isso-q.adeo.no:443/isso/oauth2
BIDRAG_ARKIV_URL: https://bidrag-dokument-arkiv.nais.preprod.local
BIDRAG_SAK_URL: https://bidrag-dokument-sak.nais.preprod.local
JOURNALPOST_URL: https://bidrag-dokument-journalpost.nais.preprod.local

FASIT_ENVIRONMENT_NAME: q1

---

####################################################################
# 
# Profiles: live
#							
####################################################################

spring:
  profiles: live

# OIDC          
no.nav.security.oidc:
  issuers: isso
  issuer:
    isso:
      acceptedaudience: bidrag-ui-master-${FASIT_ENVIRONMENT_NAME}, bidrag-dokument-ui-${FASIT_ENVIRONMENT_NAME}
      discoveryurl: ${ISSO_ISSUER_URL}/.well-known/openid-configuration

---

####################################################################
# 
# Profiles: test, secure-test
#
# Use test token
#							
####################################################################

spring:
  profiles: test, secure-test

no.nav.security.oidc:
  issuers: isso
  issuer.isso:
    discoveryurl: http://metadata
    accepted_audience: aud-localhost
    cookie_name: localhost-idtoken

---