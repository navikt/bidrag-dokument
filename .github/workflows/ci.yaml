name: continious integration
on:
  push:
    branches:
      - '**'

jobs:

  buildmaven:
    name: Build maven component
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-java@v1
        with:
          java-version: '13'
      - run: env
      - uses: actions/cache@v1
        with:
          path: ~/.m2
          key: ${{ runner.os }}-maven-${{ hashFiles('**/pom.xml') }}
          restore-keys: |
            ${{ runner.os }}-maven-
      - uses: navikt/bidrag-actions/setup-maven@v1.0.0
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
      - name: Verify that no dependencies are of type SNAPSHOT
        uses: navikt/bidrag-actions/verify-mvn-dependencies@v1.0.0
      - name: Build & test with maven
        run: mvn -B install

  builddocker:
    runs-on: ubuntu-latest
    name: Build docker image
    needs: buildmaven

    steps:
      - uses: actions/checkout@v2
      - name: Build docker image
        run: |
          echo "docker.pkg.github.com"/"$GITHUB_REPOSITORY"/"$(echo $GITHUB_REPOSITORY | sed 's;^.*/;;')" > .docker-image
          docker build -t $(cat .docker-image):$(git rev-parse --short HEAD) .
      - name: Login to Github Package Registry
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: docker login docker.pkg.github.com -u ${GITHUB_REPOSITORY} -p ${GITHUB_TOKEN}
      - name: Push Docker image
        run: docker push "$(cat .docker-image):$(cat .docker-tag)"

  deploy:
    runs-on: ubuntu-latest
    name: Deploy docker image
    needs: builddocker

    steps:
      - name: Deploy to prod
        if: github.ref == 'refs/heads/release'
        run: echo "todo - deploy to prod"
      - name: Deploy to pre-prod (q4)
        if: github.ref == 'refs/heads/master'
        run: echo "todo - deplot to preprod (q4)"
      - name: Deploy to pre-prod (q0 - default)
        if: github.ref == 'refs/heads/develop'
        run: echo "todo - deplot to preprod (q0 - default)"
      - name: Deploy to pre-prod (q1)
        if: github.ref != 'refs/heads/develop' && github.ref != 'refs/heads/master' && github.ref != 'refs/heads/release'
        run: echo "todo - deplot to preprod (q1)"
